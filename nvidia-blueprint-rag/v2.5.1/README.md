# nvidia-blueprint-rag

Generated by SUSE AI Factory

## Metadata
- **Name**: nvidia-blueprint-rag
- **Version**: v2.5.1
- **Description**: An end to end Helm chart for the NVIDIA RAG Blueprint
- **Support**: opensource
- **Tier**: certified
- **Status**: new
- **Category**: Inference

## Authors
- alessandro.festa1@suse.com (Owner)

## Stack Composition
This stack includes the following components:

- **Milvus** (milvus): Milvus is a high-performance vector database built for scale. It is used by AI applications to organize and search through large amount of unstructured data, such as text, images, and multi-modal information. (Version: )
- **OpenTelemetry Collector** (opentelemetry-collector): The OpenTelemetry Collector offers a vendor-agnostic implementation on how to receive, process and export telemetry data. In addition, it removes the need to run, operate and maintain multiple agents/collectors in order to support open-source telemetry data formats (e.g. Jaeger, Prometheus, etc.) to multiple open-source or commercial back-ends. (Version: )

Additional dependencies:

- **nv-ingest** (Version: 26.1.2)

## Installation
To install this stack on your Kubernetes cluster, ensure you have Helm installed and run:

```bash
helm upgrade --install nvidia-blueprint-rag ./chart -n nvidia-blueprint-rag --create-namespace
```

## Upgrade
To upgrade an existing installation with new values or a newer version of the chart:

```bash
helm upgrade nvidia-blueprint-rag ./chart -n nvidia-blueprint-rag
```

## Teardown
To remove the stack and all its components from your cluster:

```bash
helm uninstall nvidia-blueprint-rag -n nvidia-blueprint-rag
```
