# suse-demo

Generated by SUSE AI Factory

## Metadata
- **Name**: suse-demo
- **Version**: 0.0.2
- **Description**: A simple description
- **Support**: opensource
- **Tier**: certified
- **Status**: new
- **Category**: Inference

## Authors
- alessandro.festa1@suse.com (Owner)

## Stack Composition
This stack includes the following components:

- **Milvus** (milvus): Milvus is a high-performance vector database built for scale. It is used by AI applications to organize and search through large amount of unstructured data, such as text, images, and multi-modal information. (Version: )
- **** (google-gemma-2-2b-it): Inference model: google/gemma-2-2b-it (Version: )
- **Open WebUI** (open-webui): Open WebUI is an extensible, feature-rich, and user-friendly self-hosted WebUI designed to operate entirely offline. It supports various LLM runners, including Ollama and OpenAI-compatible APIs. (Version: )

## Installation
To install this stack on your Kubernetes cluster, ensure you have Helm installed and run:

```bash
helm upgrade --install suse-demo ./chart -n suse-demo --create-namespace
```

## Upgrade
To upgrade an existing installation with new values or a newer version of the chart:

```bash
helm upgrade suse-demo ./chart -n suse-demo
```

## Teardown
To remove the stack and all its components from your cluster:

```bash
helm uninstall suse-demo -n suse-demo
```
